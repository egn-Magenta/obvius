%# $Id$
%if($subscription_exists) {
    <p>Du abonnerer allerede på det angivne dokument.</p>
%} elsif($subscribe_error) {
    <p>Du kunne ikke abonnere på det angivne dokument på grund af følgende fejl:</p>
    <% $subscribe_error %><br>
%} else {
    <p>Du abonnerer nu på "<% $added_title %>".</p>
%}
%if($add_url) {
    <p>Klik <a href="<% $prefix . $add_url %>" title="Tilbage">her</a> for at vende tilbage.</p>
%}
<%args>
$output
</%args>
<%init>
my $subscription_exists = $output->param('subscription_exists');
my $subscribe_error = $output->param('subscribe_error');
my $added_title = $output->param('add_title');
my $add_url = $output->param('add_one_url');
</%init>